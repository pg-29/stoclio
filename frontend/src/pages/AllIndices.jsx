import { COLORS } from "../utils/market";
function n(value) { return Number.isFinite(Number(value)) ? Number(value).toLocaleString("en-IN", { maximumFractionDigits: 2 }) : "Market Closed"; }
export default function AllIndices({ indices = [], news = false }) { return <main className="terminal-page"><div className="terminal-container"><section className="terminal-section"><div className="terminal-section-title"><span>{news ? "Market Notes" : "All Indices"}</span><span className="terminal-kicker">{news ? "SESSION BRIEF" : `${indices.length} TRACKED`}</span></div>{news ? <div className="terminal-grid terminal-grid-2">{indices.slice(0, 4).map((item) => <article className="terminal-card terminal-card-inner" key={item.symbol}><strong>{item.symbol}</strong><p className="terminal-kicker" style={{ marginTop: 8 }}>Latest market movement: {n(item.price)}. Review price action and volume before making decisions.</p></article>)}</div> : <div className="terminal-table-wrap"><table className="terminal-table"><thead><tr><th>Name</th><th>LTP</th><th>Open</th><th>High</th><th>Low</th><th>Close</th><th>Change</th></tr></thead><tbody>{indices.length ? indices.map((item) => { const change = Number(item.price) - Number(item.open); return <tr key={item.symbol}><td>{item.symbol}</td><td className={change >= 0 ? "positive" : "negative"}>{n(item.price)}</td><td>{n(item.open)}</td><td>{n(item.high)}</td><td>{n(item.low)}</td><td>{n(item.prev || item.price)}</td><td className={change >= 0 ? "positive" : "negative"}>{Number.isFinite(change) ? `${(change / item.open * 100).toFixed(2)}%` : "Market Closed"}</td></tr>; }) : <tr><td colSpan="7" className="closed">Loading Market Data</td></tr>}</tbody></table></div>}</section></div></main>; }
