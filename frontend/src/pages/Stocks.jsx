import { useState } from "react";
import { Flame } from "lucide-react";
import TradingViewChart from "../components/TradingViewChart.jsx";
import InstrumentTable from "../components/InstrumentTable.jsx";
import { ChangeTag } from "../components/MarketShared.jsx";
import { COLORS, fontBody, fontDisplay, fontMono, inr, pctChange } from "../utils/market";
export default function Stocks({ stocks, initialSymbol }) { const [selected, setSelected] = useState(initialSymbol || stocks[0]?.symbol); const chosen = stocks.find((item) => item.symbol === selected) || stocks[0]; const sorted = [...stocks].sort((a, b) => pctChange(b.price, b.open) - pctChange(a.price, a.open)); return <div style={{ background: COLORS.paper }}><div className="lv-page-wrap"><h1 style={{ fontFamily: fontDisplay, fontWeight: 600, fontSize: 26, color: COLORS.ink, margin: "0 0 6px" }}>Stocks</h1><p style={{ fontFamily: fontBody, fontSize: 13.5, color: COLORS.inkMuted, margin: "0 0 20px" }}>NSE-listed equities. Chart data streams live from TradingView; tap any row to load its chart.</p>{chosen && <div style={{ background: COLORS.paperCard, border: `1px solid ${COLORS.line}`, borderRadius: 14, padding: 14, marginBottom: 18 }}><div style={{ display: "flex", justifyContent: "space-between", alignItems: "baseline", marginBottom: 10 }}><div><div style={{ fontFamily: fontDisplay, fontWeight: 600, fontSize: 16, color: COLORS.ink }}>{chosen.symbol}</div><div style={{ fontFamily: fontBody, fontSize: 12, color: COLORS.inkMuted }}>{chosen.name} · {chosen.sector}</div></div><div style={{ textAlign: "right" }}><div style={{ fontFamily: fontMono, fontSize: 17, color: COLORS.ink }}>{inr(chosen.price)}</div><ChangeTag change={pctChange(chosen.price, chosen.open)} /></div></div><TradingViewChart symbol={chosen.symbol} height={360} /></div>}<div className="lv-movers-grid" style={{ marginBottom: 18 }}>{[sorted.slice(0, 5), sorted.slice(-5).reverse()].map((group, index) => <div key={index} style={{ background: COLORS.paperCard, border: `1px solid ${COLORS.line}`, borderRadius: 14, padding: 16 }}><div style={{ display: "flex", alignItems: "center", gap: 7, marginBottom: 10, fontFamily: fontDisplay, fontWeight: 600, fontSize: 13.5, color: COLORS.ink }}><Flame size={14} color={index ? COLORS.down : COLORS.up} /> Top 5 {index ? "losers" : "gainers"} today</div>{group.map((item) => <div key={item.symbol} style={{ display: "flex", justifyContent: "space-between", padding: "6px 0" }}><span style={{ fontFamily: fontBody, fontSize: 12.5, color: COLORS.ink }}>{item.symbol}</span><ChangeTag change={pctChange(item.price, item.open)} size={12} /></div>)}</div>)}</div><InstrumentTable title="All tracked stocks" items={stocks} onSelect={setSelected} /></div></div>; }
