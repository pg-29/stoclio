import { useState } from "react";
import TradingViewChart from "../components/TradingViewChart.jsx";
import InstrumentTable from "../components/InstrumentTable.jsx";
import { ChangeTag, MarketStatusBadge } from "../components/MarketShared.jsx";
import { COLORS, fontBody, fontDisplay, fontMono, inr, pctChange } from "../utils/market";
export default function Commodities({ commodities, initialSymbol }) { const [selected, setSelected] = useState(initialSymbol || commodities[0]?.symbol); const chosen = commodities.find((item) => item.symbol === selected) || commodities[0]; return <div style={{ background: COLORS.paper }}><div className="lv-page-wrap"><h1 style={{ fontFamily: fontDisplay, fontWeight: 600, fontSize: 26, color: COLORS.ink, margin: "0 0 6px" }}>Commodities</h1><p style={{ fontFamily: fontBody, fontSize: 13.5, color: COLORS.inkMuted, margin: "0 0 4px" }}>MCX commodity futures. Chart data streams live from TradingView.</p><p style={{ fontFamily: fontBody, fontSize: 11.5, color: COLORS.inkMuted, margin: "0 0 20px" }}>Live prices as of {new Date().toLocaleDateString("en-IN", { day: "2-digit", month: "short", year: "numeric" })} <MarketStatusBadge compact light /></p>{chosen && <div style={{ background: COLORS.paperCard, border: `1px solid ${COLORS.line}`, borderRadius: 14, padding: 14, marginBottom: 18 }}><div style={{ display: "flex", justifyContent: "space-between", alignItems: "baseline", marginBottom: 10 }}><div><div style={{ fontFamily: fontDisplay, fontWeight: 600, fontSize: 16, color: COLORS.ink }}>{chosen.symbol}</div><div style={{ fontFamily: fontBody, fontSize: 12, color: COLORS.inkMuted }}>MCX · continuous future{chosen.unit ? ` · ${chosen.unit}` : ""}</div></div><div style={{ textAlign: "right" }}><div style={{ fontFamily: fontMono, fontSize: 17, color: COLORS.ink }}>{inr(chosen.price)}</div><ChangeTag change={pctChange(chosen.price, chosen.open)} /></div></div><TradingViewChart symbol={chosen.symbol} height={360} /></div>}<InstrumentTable title="Commodities" items={commodities} onSelect={setSelected} /></div></div>; }
